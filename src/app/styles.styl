html {
    font-size: 75%;
}

body {
    font-size: 1rem;
    line-height: 1.5;
    font-family: 'Open Sans', 'Helvetica', 'Arial', sans-serif;
    font-stretch: normal;
    color: #334152;
    letter-spacing: 0.4px;
    overflow-y: scroll;
    position: relative;
    margin 0
}
.main
    display flex
.content
    width 100%